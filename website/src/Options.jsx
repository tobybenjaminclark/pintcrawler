import { Link, useLocation } from "react-router-dom";

export default function Options({isOptionVisible}){

    const range = 0;  // kilometers 
    const rating = 0; // 0 - lowest rating, 1 - highest rating
    const fatometer = 0; // 0 - least walking, 1 - meadium, 2 - most

    

    return (
        isOptionVisible && (
        <div>
            
            <h1>
                hellooo
            </h1>


        </div>
        )
    );
}
